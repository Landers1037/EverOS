<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

<!-- BEGIN:everos-uiux-design-spec -->
# EverOS UI/UX 设计规范

本规范用于指导 EverOS 内所有 agent 在进行页面、组件、交互和视觉实现时保持统一。除非用户明确指定新的视觉方向，否则默认遵循本规范，不自行引入与本规范冲突的颜色、材质、圆角、间距、按钮风格或动画语言。

## 1. 设计定位

- 产品形态：极简 WebOS
- 视觉气质：克制、安静、现代、偏科技感
- 核心关键词：银灰、黑、半透明、毛玻璃、悬浮层级、简洁排版、滑动过渡
- 禁止事项：霓虹发光边框、过度装饰、强拟物、高饱和撞色、夸张阴影、复杂纹理背景

## 2. 视觉原则

- 整体以深色底为主，银灰作为前景和结构色，强调沉稳和秩序感。
- 面板采用半透明毛玻璃材质，但透明度要克制，优先保证内容可读性。
- 所有组件保持中等圆角，不使用过尖锐或过度圆润的形态。
- 按钮、输入框、窗口、卡片都应体现“轻悬浮”而不是“重投影”。
- 排版保持简洁，留白充分，避免信息密度失控。
- 动效统一为滑动和淡入淡出，不使用弹跳、旋转、果冻、强缩放等高存在感动画。

## 3. 色彩系统

说明：由于当前无法直接读取设计稿图片中的精确颜色，本规范基于你提供的“银灰 + 黑”原型方向建立标准化 token。后续若需要，可在不改变结构的前提下微调数值。

### 3.1 基础颜色 Token

- `--bg-base: #08090B`
  系统最深背景，用于桌面主背景、全屏底色。
- `--bg-elevated: #111317`
  一级抬升背景，用于窗口、Dock、导航区域。
- `--bg-panel: rgba(255, 255, 255, 0.08)`
  毛玻璃面板基础色，用于卡片、浮层、悬浮容器。
- `--bg-panel-hover: rgba(255, 255, 255, 0.12)`
  面板 hover 态。
- `--bg-panel-active: rgba(255, 255, 255, 0.16)`
  面板激活态。
- `--bg-input: rgba(255, 255, 255, 0.06)`
  输入框默认背景。
- `--bg-input-focus: rgba(255, 255, 255, 0.10)`
  输入框聚焦背景。

- `--text-primary: #F3F4F6`
  一级文本，标题和主要内容。
- `--text-secondary: #B7BDC7`
  二级文本，说明、元数据、次级标签。
- `--text-tertiary: #8A9099`
  三级文本，弱提示、占位符。
- `--text-disabled: #666C75`
  禁用文本。

- `--border-subtle: rgba(255, 255, 255, 0.10)`
  常规边框。
- `--border-default: rgba(255, 255, 255, 0.14)`
  面板/输入框默认边框。
- `--border-strong: rgba(255, 255, 255, 0.20)`
  激活或强调边框。

- `--fill-solid: #D7DBE0`
  极简实心按钮主填充。
- `--fill-solid-hover: #E2E5E9`
  实心按钮 hover。
- `--fill-solid-active: #C4C9D0`
  实心按钮按下态。
- `--fill-solid-contrast: #0C0E12`
  实心浅色按钮上的深色文本。

- `--state-info: #AAB4C3`
- `--state-success: #8FAE9A`
- `--state-warning: #C0A97C`
- `--state-danger: #B98B8B`

### 3.2 背景层级规则

- 桌面背景使用纯深色或深色渐变，不能比窗口更亮。
- 窗口容器使用 `--bg-elevated` 或 `--bg-panel`，根据是否需要毛玻璃效果决定。
- 同一界面内层级最多 3 层，避免多层叠加透明导致发灰和发脏。
- 毛玻璃只用于“容器”，不用于正文文本底部和密集列表行。

### 3.3 文本对比规则

- 标题、当前窗口名、主要数字信息使用 `--text-primary`。
- 正文、表单内容、导航项默认使用 `--text-secondary`。
- placeholder、注释、说明性文案使用 `--text-tertiary`。
- 禁止大面积使用纯白；文本应维持柔和银灰白，避免刺眼。

## 4. 材质与容器规范

### 4.1 毛玻璃面板

- 推荐背景：`rgba(255, 255, 255, 0.08)`
- 推荐边框：`1px solid rgba(255, 255, 255, 0.10)`
- 推荐模糊：`backdrop-filter: blur(20px)`
- 推荐阴影：`0 10px 30px rgba(0, 0, 0, 0.28)`

规则：

- 只保留轻阴影，不增加外发光。
- 边框必须轻，不做高亮描边。
- 面板内容较复杂时，应降低透明度波动，优先保证信息清晰。

### 4.2 圆角规范

- `--radius-xs: 8px`
- `--radius-sm: 10px`
- `--radius-md: 14px`
- `--radius-lg: 18px`
- `--radius-xl: 22px`

使用建议：

- 输入框、按钮、小卡片：`10px` 或 `14px`
- 窗口、弹层、系统面板：`18px`
- 大型容器不要超过 `22px`

## 5. 按钮规范

### 5.1 按钮总体原则

- 默认采用极简实心按钮，不使用发光边框按钮。
- 优先通过填充、明度和对比表达层级，不通过高饱和颜色表达。
- 按钮形态简洁，避免图标、描边、文字、标签多元素同时堆叠。

### 5.2 按钮类型

#### 主按钮 Primary

- 背景：`--fill-solid`
- 文本：`--fill-solid-contrast`
- Hover：`--fill-solid-hover`
- Active：`--fill-solid-active`
- 禁用：背景降到 `rgba(255,255,255,0.10)`，文本 `--text-disabled`

适用：

- 确认、提交、创建、保存、进入下一步

#### 次按钮 Secondary

- 背景：`rgba(255,255,255,0.08)`
- 文本：`--text-primary`
- 边框：`1px solid --border-default`
- Hover：背景提升到 `rgba(255,255,255,0.12)`
- Active：背景提升到 `rgba(255,255,255,0.16)`

适用：

- 取消、返回、更多操作、次级确认

#### 幽灵按钮 Ghost

- 背景：透明
- 文本：`--text-secondary`
- Hover：`rgba(255,255,255,0.06)`
- Active：`rgba(255,255,255,0.10)`

适用：

- 工具栏操作、标题栏按钮、低强调操作

### 5.3 尺寸规范

- `--button-h-sm: 32px`
- `--button-h-md: 40px`
- `--button-h-lg: 46px`
- 水平内边距：`12px / 16px / 20px`
- 图标间距：`8px`
- 字重建议：`500` 或 `600`

规则：

- 同一视图最多使用一种主按钮尺寸作为主节奏。
- 重要区域优先 `md`，弹窗确认区可使用 `lg`。
- 按钮文本尽量短，避免一行超过 12 个汉字。

## 6. 输入框规范

### 6.1 视觉风格

- 输入框应呈悬浮态，不贴底色平铺。
- 背景使用 `--bg-input`
- 边框使用 `1px solid --border-subtle`
- 圆角使用 `--radius-md`
- 高度建议 `40px` 或 `44px`

### 6.2 状态变化

- 默认：低对比背景 + 轻边框
- Hover：背景略亮，不改动大结构
- Focus：背景切换为 `--bg-input-focus`，边框提升到 `--border-strong`
- Error：边框切换为 `--state-danger`

规则：

- 聚焦态只做轻微强化，不使用高亮光圈。
- placeholder 一律使用 `--text-tertiary`。
- 搜索框、地址栏、命令输入框都遵守同一输入体系。

## 7. 间距规范

### 7.1 8pt 间距系统

- `--space-1: 4px`
- `--space-2: 8px`
- `--space-3: 12px`
- `--space-4: 16px`
- `--space-5: 20px`
- `--space-6: 24px`
- `--space-8: 32px`
- `--space-10: 40px`
- `--space-12: 48px`

### 7.2 使用规则

- 同类组件内部间距优先使用 `8 / 12 / 16`。
- 卡片或面板内边距优先使用 `16 / 20 / 24`。
- 页面级模块之间使用 `24 / 32 / 40`。
- 窗口内容区不要同时出现超过 4 种间距值。
- 图标与文字常规间距使用 `8px`。
- 表单项垂直间距建议 `12px` 或 `16px`。

### 7.3 布局密度

- WebOS 场景优先中密度布局，不做过松或过挤。
- 信息较多的窗口通过分区和留白解决，不通过无限缩小字号解决。
- Dock、顶部栏、侧边栏应维持统一节奏，不出现局部特别拥挤的区域。

## 8. 排版规范

### 8.1 字体气质

- 排版方向：简洁、理性、轻科技感
- 避免花哨字体、过强几何感字体、过宽字重对比
- 数字、时间、系统状态信息可适度使用等宽数字特性

### 8.2 字号层级

- `--text-xs: 12px`
- `--text-sm: 13px`
- `--text-md: 14px`
- `--text-lg: 16px`
- `--text-xl: 20px`
- `--text-2xl: 24px`

使用建议：

- 系统辅助信息：`12px` 或 `13px`
- 正文和按钮：`14px`
- 模块标题：`16px`
- 页面标题 / 窗口标题：`20px` 或 `24px`

规则：

- 一个页面主字号不要超过 3 种。
- 行高建议保持在 `1.4` 到 `1.6`。
- 标题优先靠字重和对比提升层级，不依赖过大字号。

## 9. 动画规范

### 9.1 动效原则

- 统一采用滑动过渡和轻淡入淡出。
- 动效服务于空间关系表达，不做展示型炫技动画。
- 所有动效都必须短、稳、轻。

### 9.2 时长规范

- `--motion-fast: 140ms`
- `--motion-base: 200ms`
- `--motion-slow: 280ms`

推荐：

- Hover：`140ms`
- 按钮按下反馈：`140ms`
- 面板展开/收起：`200ms`
- 窗口切换、抽屉进出：`240ms` 到 `280ms`

### 9.3 缓动函数

- 推荐主缓动：`cubic-bezier(0.22, 1, 0.36, 1)`
- 轻量交互：`ease-out`

### 9.4 动画类型

允许：

- 水平滑入滑出
- 垂直轻滑动
- 透明度渐变
- 轻微位移过渡

禁止：

- 弹跳
- 大幅缩放
- 3D 翻转
- 持续闪烁
- 高亮扫光

## 10. 窗口与系统界面规范

- 窗口是 EverOS 的核心容器，应保持统一的圆角、透明度和内边距体系。
- 标题栏高度建议 `44px` 到 `52px`。
- 窗口内容区默认内边距建议 `20px` 或 `24px`。
- 窗口边界通过轻边框和阴影区分，不使用高亮描边。
- 多窗口并存时，通过透明度、模糊、阴影和层级关系体现焦点，不通过艳色强调。

## 11. Agent 执行规则

当 agent 生成 UI、样式或组件时，必须遵守以下规则：

- 优先复用本规范中的颜色 token，不自行发明新的主色体系。
- 所有新组件默认使用中等圆角和低对比轻边框。
- 表单控件、按钮、面板必须保持统一材质语言。
- 如无明确要求，默认使用深色主题，不额外生成浅色主题版本。
- 不增加霓虹、赛博、渐变发光等未被授权的视觉元素。
- 动效统一使用滑动 + 渐变，不引入跳跃式动画。
- 优先保证可读性和层级关系，其次才是视觉表现。

## 12. 实现建议

推荐在项目中沉淀以下设计 token：

- `colors.background.*`
- `colors.text.*`
- `colors.border.*`
- `colors.fill.*`
- `radius.*`
- `spacing.*`
- `motion.duration.*`
- `motion.easing.*`

推荐所有组件基于 token 实现，禁止在业务组件里随意写死颜色值和动画参数。
<!-- END:everos-uiux-design-spec -->
