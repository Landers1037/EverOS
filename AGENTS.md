<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

<!-- BEGIN:everos-uiux-design-spec -->
# EverOS UI/UX 设计规范（基于当前原型图升级）

本规范用于指导 EverOS 内所有 agent 在实现页面、组件、系统界面和主题时保持统一。当前原型图的核心特征不是传统强科技感 WebOS，而是“中性色极简仪表盘 + 轻磨砂悬浮层 + 高秩序信息布局”。除非用户明确指定新的视觉方向，否则默认遵循本规范。

## 1. 原型图风格分析

### 1.1 整体风格定义

- 风格流派：`Neo-Minimal Dashboard`、`Soft Glass Minimalism`、`Neutral Enterprise`
- 视觉气质：安静、理性、精致、现代、克制、轻商务
- 核心关键词：浅灰、米白、石墨黑、低饱和中性色、薄边框、柔和阴影、轻磨砂、卡片矩阵、弱装饰
- 非本风格元素：霓虹渐变、赛博发光、重玻璃、高饱和撞色、强拟物、过重阴影、夸张动画

### 1.2 风格判断结论

- 该原型属于“中性色极简后台设计语言”，接近现代 SaaS/Admin Dashboard，而不是拟物 OS 或赛博风桌面。
- 画面重点依赖：留白、秩序、模块化卡片、低对比边框、克制材质层次，而不是依赖高色彩冲击。
- 信息表达方式偏“结构化展示”，适合窗口、侧栏、系统面板、设置页、文件管理器等复杂功能界面。

## 2. 色彩系统分析

### 2.1 配色逻辑

- 以中性色为主，颜色关系是“背景灰阶 + 文字灰阶 + 极少量功能色”。
- 主视觉不是靠彩色品牌色驱动，而是靠“黑白灰层级”建立专业感。
- 点缀色只用于选中、数据强调、状态提示，不应大面积铺满界面。
- 正常信息层级优先用明度差，功能状态才使用色相差。

### 2.2 色彩角色

- 主色：石墨黑 / 深炭灰
  - 用于核心标题、主要数字、主按钮、深色主题下的高对比内容
- 辅助色：浅灰、雾灰、暖灰、银灰
  - 用于面板、输入框、卡片底、悬浮容器、分区背景
- 点缀色：低饱和蓝灰
  - 用于选中态、强调态、当前焦点、轻量链接和系统提示
- 功能色：
  - 成功：偏鼠尾草绿，不用荧光绿
  - 警告：偏琥珀棕，不用高亮黄
  - 危险：偏灰玫瑰红，不用纯正警示红
- 中性色：
  - 用于背景层级、文本层级、分割线、禁用态、表格结构线

### 2.3 明亮主题建议色卡

- `--bg-base: #EFEBE5`
- `--bg-canvas: #F6F3EE`
- `--bg-elevated: rgba(255,255,255,0.82)`
- `--bg-panel: rgba(255,255,255,0.62)`
- `--bg-panel-strong: rgba(255,255,255,0.90)`
- `--bg-input: rgba(42,37,31,0.05)`
- `--text-primary: #1C1917`
- `--text-secondary: #696257`
- `--text-tertiary: #9A9287`
- `--border-subtle: rgba(42,37,31,0.08)`
- `--border-default: rgba(42,37,31,0.11)`
- `--accent: #73879A`
- `--accent-muted: rgba(115,135,154,0.14)`

### 2.4 暗黑主题建议色卡

- `--bg-base: #0D1117`
- `--bg-canvas: #11161D`
- `--bg-elevated: rgba(20,25,33,0.86)`
- `--bg-panel: rgba(255,255,255,0.05)`
- `--bg-panel-strong: rgba(255,255,255,0.08)`
- `--bg-input: rgba(255,255,255,0.06)`
- `--text-primary: #F4F1EB`
- `--text-secondary: #C9C2B7`
- `--text-tertiary: #968F85`
- `--border-subtle: rgba(255,255,255,0.08)`
- `--border-default: rgba(255,255,255,0.12)`
- `--accent: #A7B4C3`
- `--accent-muted: rgba(167,180,195,0.16)`

### 2.5 色彩层级关系

- 背景层：`base -> canvas -> elevated`
- 容器层：`panel -> panel-strong -> selected/accent-muted`
- 文本层：`primary -> secondary -> tertiary -> disabled`
- 结构层：`subtle border -> default border -> strong border`
- 状态层：状态色只点到为止，不可压过主体中性色结构

## 3. 视觉元素分析

### 3.1 圆角规范

- `--radius-xs: 8px`
- `--radius-sm: 10px`
- `--radius-md: 14px`
- `--radius-lg: 18px`
- `--radius-xl: 24px`
- `--radius-2xl: 28px`

使用建议：

- 输入框、切换按钮、小工具按钮：`10px` 到 `14px`
- 卡片、列表项、内容区块：`14px` 到 `18px`
- 浮层、窗口、Dock、设置面板：`18px` 到 `24px`
- 不使用过大的胶囊圆角去削弱秩序感

### 3.2 边框样式

- 边框统一为 `1px`
- 采用低对比半透明描边，不做高亮描边
- 分割线应比外框更轻
- 在明亮主题中优先使用暖灰透明边框，在暗黑主题中优先使用白色低透明边框
- 禁止增加边框高光、内发光、顶部亮边这类显性装饰

### 3.3 阴影规范

- 阴影风格：垂直柔和扩散阴影
- 不使用浓重投影，也不使用彩色阴影
- 推荐层级：
  - 小卡片：`0 8px 20px rgba(...)`
  - 普通浮层：`0 14px 34px rgba(...)`
  - 窗口与主面板：`0 24px 60px rgba(...)`
  - 聚焦窗口：`0 32px 90px rgba(...)`

### 3.4 间距留白

- 采用 `4 / 8 / 12 / 16 / 24 / 32 / 40 / 48` 体系
- 同一视图中尽量不要超过 4 种间距值
- 组件内边距优先 `12 / 16 / 20`
- 模块之间优先 `24 / 32`
- 页面大区块之间优先 `32 / 40`

### 3.5 字体与数字

- 字体方向：Inter / Geist / SF Pro / Segoe UI 同类现代无衬线
- 字重策略：
  - 标题 `600`
  - 模块名 `600`
  - 正文 `400` 到 `500`
  - 辅助信息 `400`
- 推荐字号：
  - `12px` 系统说明
  - `13px` 次级标签
  - `14px` 正文与按钮
  - `16px` 模块标题
  - `20px` 到 `28px` 页面/窗口标题
- 数字、时间、数据看板优先支持等宽数字

### 3.6 图标风格

- 使用统一线性图标系统，如 `Lucide`
- 推荐描边 `1.5` 到 `1.75`
- 同层级图标大小统一，不混用实心和描边图标
- 图标颜色默认跟随 `text-secondary`，选中时才切到 `accent` 或 `text-primary`

### 3.7 分割线质感

- 分割线必须弱化，只用于结构提示
- 表格线、列表线、侧栏分隔统一采用低对比线
- 禁止明显发光线、重边框、双层线

## 4. 排版与布局分析

### 4.1 布局特征

- 采用强结构化布局：顶部状态栏 + 侧栏导航 + 卡片/内容区
- 大面积信息通过卡片和分区管理，而不是靠不同颜色块硬切
- 容器内对齐整齐，模块边界清晰，卡片之间留白均匀

### 4.2 栅格建议

- 桌面 Web 推荐 12 栅格或等比分区布局
- KPI 卡片采用 `3` 或 `4` 列
- 主内容区优先使用 `16px / 24px` 统一 gutter
- 表单页与设置页优先左右两栏或“标签列 + 内容列”

### 4.3 层级关系

- 一级层级：页面背景 / 桌面背景
- 二级层级：窗口 / 主容器 / 系统面板
- 三级层级：卡片 / 列表行 / 输入控件
- 同一界面最多保留 3 个可感知层级，避免透明叠透明导致发灰发脏

## 5. 材质与细节分析

### 5.1 渐变

- 渐变不是默认必选项，而是可切换材质表达
- 当前项目允许两种 UI 风格：
  - `Gradient`：保留极轻背景氛围和弱渐变材质
  - `Minimal`：去掉渐变、高光和额外装饰，只保留纯色层级、边框和阴影
- 即使在 `Gradient` 模式下，渐变也只能作为极弱辅助，不可成为视觉主体
- 禁止高饱和彩虹渐变、强品牌渐变、明显扫光

### 5.2 磨砂效果

- 可用于系统栏、Dock、窗口、菜单、弹层
- 透明度必须克制，优先可读性
- 推荐：`backdrop-filter: blur(20px ~ 24px)`
- 应支持 `全局透明度` 调节，并允许侧边栏与 Dock 透明度作为局部覆盖项
- 所有透明度调节只影响背景 alpha，不降低文字与图标可读性

### 5.3 浮雕与线条

- 本风格不是拟物风，不使用重浮雕
- 默认不使用边框高光和顶部亮边；若保留渐变风格，也只能是非常轻的材质过渡
- 线条以“柔和边界”存在，而不是视觉主角

### 5.4 材质感总结

- 材质关键词：磨砂玻璃、细哑光、轻悬浮、纸感中性色、空气感留白
- 质感来源：边框 + 柔影 + 微高光 + 层级秩序
- 不是靠强反射、金属拉丝或重纹理实现高级感

## 6. EverOS 统一设计规范

### 6.1 产品定位

- 产品形态：桌面式 WebOS / 多窗口界面
- 默认风格：中性色极简系统界面
- 视觉目标：让 WebOS 既有工具产品的专业感，也保留系统界面的悬浮感

### 6.2 组件规范

- 所有新组件必须基于 token 实现，不直接在业务组件内随意写死颜色
- 顶栏、侧栏、Dock、窗口、弹层必须共享相同圆角、边框、阴影和磨砂逻辑
- 表单控件、列表、卡片、表格行都保持统一中性材质
- 选中态优先使用 `accent-muted`，而不是高饱和纯色底
- 主按钮使用高对比实心，次按钮使用透明或浅底边框
- 标准模式 Dock 保持全宽贴底布局；Mini 模式才使用悬浮短 Dock
- 标准模式 Dock 需要避开左侧边栏宽度，不覆盖主导航区域
- 标准模式 Dock 应保留与当前规范一致的顶部圆角，形成克制的系统容器边界
- 顶栏、侧栏圆角保持克制，避免过大圆角削弱专业感

### 6.3 主题规范

- 必须同时维护明亮、暗黑两套主题，不允许只做单主题推断
- 两套主题保持同一种层级关系，不做简单颜色反相
- 亮色主题以“米灰白 + 石墨黑 + 蓝灰点缀”为主
- 暗色主题以“深炭灰 + 暖白字 + 银灰点缀”为主

### 6.4 动效规范

- 统一使用 `140ms / 220ms / 300ms`
- 统一缓动：`cubic-bezier(0.22, 1, 0.36, 1)`
- 允许：轻位移、淡入淡出、抽屉滑动、窗口微缩放
- 禁止：弹跳、果冻、3D 翻转、大幅缩放、无意义连续闪烁

### 6.5 风格切换规范

- 外观设置中必须支持 `Gradient` 与 `Minimal` 两种 UI 风格切换
- `Gradient` 风格：允许极弱的背景氛围、面板材质渐变和微弱高低明度过渡
- `Minimal` 风格：移除渐变、高光、装饰性 overlay，仅保留中性色层级、边框、阴影和留白
- 两种风格必须共用同一套色彩、圆角、间距、阴影 token，区别只在材质表达

## 7. Agent 执行规则

当 agent 生成 UI、样式或组件时，必须遵守以下规则：

- 优先复用 `background / text / border / accent / state / radius / spacing / motion` token
- 新增 UI 默认采用本规范的中性色极简风，不额外引入赛博、霓虹、卡通或强品牌化视觉
- 任何页面都先建立层级，再谈装饰；优先解决可读性、留白、边界、对齐
- 图标统一使用线性图标集，不使用 emoji 充当结构图标
- 不在同一界面中混用多套圆角和阴影风格
- 不在业务组件里大面积写死十六进制颜色和随机阴影值
- 亮暗主题都必须检查文本对比、边框可见性和状态反馈
- 若无用户特别指定，优先保持 `Minimal` 取向，不主动叠加强渐变与高光效果

## 8. 实现建议

推荐在项目中沉淀以下 token：

- `colors.background.*`
- `colors.surface.*`
- `colors.text.*`
- `colors.border.*`
- `colors.state.*`
- `colors.accent.*`
- `shadow.*`
- `radius.*`
- `spacing.*`
- `motion.duration.*`
- `motion.easing.*`

推荐沉淀以下通用样式能力：

- `ui-surface`
- `ui-surface-soft`
- `ui-card`
- `ui-input`
- `ui-control`
- `ui-control-active`
- `ui-divider`
- `html.ui-minimal`

后续所有 EverOS 页面与组件默认基于本规范继续扩展。
<!-- END:everos-uiux-design-spec -->
